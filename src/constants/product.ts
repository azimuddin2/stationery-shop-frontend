const categoryNames = [
  'Writing',
  'Office Supplies',
  'Art Supplies',
  'Educational',
  'Technology',
];

export const categoryOptions = categoryNames.map((item) => ({
  value: item,
  label: item,
}));
